<%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
  <div class="card-block">
    <h5 class="card-title">Resend unlock instructions</h5>
    <%= devise_error_messages! %>
    <div class="form-group row">
      <label class="col-sm-3 text-sm-right">
        <%= f.label :email %>
      </label>
      <div class="col-sm-9">
        <%= f.email_field :email, class: 'form-control', autofocus: true %>
      </div>
    </div>
    <div class="row">
      <div class="col-sm-5 push-sm-7 text-sm-right">
        <%= f.submit "Resend instructions", :class => 'btn btn-primary' %>
      </div>
      <div class="col-sm-7 pull-sm-5 small">
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
<% end %>
